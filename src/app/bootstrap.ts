// This file provides a theme-switching function for Bootstrap
export function initBootstrap() {
  if (typeof window !== 'undefined') {
    // Add any client-side Bootstrap initialization code here
    // The actual Bootstrap JS is already loaded via CDN in layout.tsx
  }
} 